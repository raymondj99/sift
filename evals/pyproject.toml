[project]
name = "sift-evals"
version = "0.1.0"
description = "Memory-system evaluation harness for Cortex (LoCoMo, CodingMem)"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.42",
    "numpy>=1.26",
    "tqdm>=4.66",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]
