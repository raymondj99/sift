[project]
name = "sift-bench"
version = "0.1.0"
description = "External benchmark suite for the Cortex memory system"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.42",
    "numpy>=1.26",
    "tqdm>=4.66",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]
